{
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "framework": "gatsby",
  "installCommand": "npm install",
  "devCommand": "cross-env NODE_OPTIONS=--openssl-legacy-provider gatsby develop --port $PORT"
}
